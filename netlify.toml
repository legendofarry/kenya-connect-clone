[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"
  # Builds the app as a Netlify SSR function bundle (.netlify/functions-internal)
  # instead of the default Cloudflare Worker output.
  NITRO_PRESET = "netlify"
